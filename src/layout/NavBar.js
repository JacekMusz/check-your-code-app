import React, { Component, createFactory } from "react";
import "../style/NavBar.css";

const NavBar = () => {
  return (
    <div className="nav-bar">
      <p className="nav-bar__bookmark-title">Code Editor</p>
    </div>
  );
};

export default NavBar;
